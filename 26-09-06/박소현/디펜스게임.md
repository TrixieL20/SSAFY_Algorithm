# 문제 접근 방식
무적권을 최대한 쓸 수 있도록 K + 1개씩 묶어서 그 중 가장 병사가 덜 필요한 라운드에 병사를 사용하자.

# 핵심 아이디어
가장 병사가 덜 필요한 라운드를 얻어올 때, Priority Queue를 사용했다.

# 복잡도
## 시간 복잡도
O(NlogK)
- while문: O(N)
   - pq 삽입 삭제: O(logK)
## 공간 복잡도
O(K)

# 풀이하면서 실수했던 부분
- 라운드 수를 현재까지 돌린 라운드 수를 리턴했어야 했는데 병사가 부족해지는 라운드 수를 리턴해서 틀렸다.
